<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSE 340</title>
    <link rel="stylesheet" href="/css/styles.css" media="screen">

    <%# Este bloque permitirá cargar hojas de estilo específicas de la página %>
        <%- locals.pageCSS %>
</head>
<%# Esta sintaxis permite añadir una clase al body desde el controlador %>

    <body class="<%= locals.bodyClass || '' %>">
        <div id="wrapper">
            <header>
                <%- include('../partials/header') %>
            </header>
            <nav>
                <%- nav %>
            </nav>
            <main>
                <%- messages() %>
                    <% if (locals.notice) { %>
                        <ul class="notice">
                            <li>
                                <%= notice %>
                            </li>
                        </ul>
                        <% } %>
                            <%- body %>
            </main>
            <footer>
                <%- include('../partials/footer') %>
            </footer>
        </div>
    </body>

</html>